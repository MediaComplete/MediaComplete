<Window x:Class="MSOE.MediaComplete.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:System.Windows.Controls;assembly=TreeViewEx"
        xmlns:customControls="clr-namespace:MSOE.MediaComplete.CustomControls"
        Title="Media Complete" Height="339" Width="787"
        AutomationProperties.Name="Tree">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MainWindowResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="189" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition />
            <RowDefinition Height="100" />
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Grid.Column="0" Margin="0,0,0,0" >
            <MenuItem Header="_File">
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Edit" Click="MenuItem_Click">
                <MenuItem Header="_Settings" />
            </MenuItem>
            <MenuItem Header="_Playlists" />
            <MenuItem Header="_MetaDataFinderThing" />
        </Menu>
        <TextBox Grid.Row="0" Grid.Column="1" Height="23" Text="Search" Width="189" HorizontalAlignment="Right" />
        <ToolBarTray Grid.Row="1" Grid.Column="0">
            <ToolBar Margin="0,0,0,161">
                <Button Name="AddFile" ToolTip="Add File" VerticalAlignment="Center" Click="AddFile_Click">
                    <Image Source="{StaticResource Toolbar-AddMusicFile-Icon}" />
                </Button>
                <Button Name="AddFolder" ToolTip="Add Folder" VerticalAlignment="Center" Click="AddFolder_Click">
                    <Image Source="{StaticResource Toolbar-AddMusicFolder-Icon}" />
                </Button>
                <Button Name="AutoIdentifyMusic" ToolTip="{StaticResource Toolbar-AutoIdMusic-Tooltip}"
                        VerticalAlignment="Center" Click="Toolbar_AutoIDMusic_Click">
                    <Image Source="{StaticResource Toolbar-AutoIdMusic-Icon}" />
                </Button>
                <Button Name="SortMusic" ToolTip="{StaticResource Toolbar-SortMusic-Tooltip}"
                        VerticalAlignment="Center" Click="Toolbar_SortMusic_Click">
                    <Image Source="{StaticResource Toolbar-SortMusic-Icon}" />
                </Button>
            </ToolBar>
        </ToolBarTray>
        <Grid Margin="0,0,0,1" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260*" />
                <ColumnDefinition Width="260*" />
                <ColumnDefinition Width="260*" />
            </Grid.ColumnDefinitions>

            <controls:TreeViewEx x:Name="FolderTree" Grid.Column="0" Margin="0,33,0,0" 
             AutomationProperties.Name="Tree" 
             MouseUp="FolderTree_OnMouseUp"
             ItemsSource="{Binding Children}">
                <controls:TreeViewEx.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}"  DataType="{x:Type customControls:FolderTreeViewItem}">
                        <TextBlock Text="{Binding}"/>
                    </HierarchicalDataTemplate>
                </controls:TreeViewEx.ItemTemplate>

            </controls:TreeViewEx>

            <controls:TreeViewEx x:Name="SongTree" Grid.Column="1" Margin="0,33,0,0" 
             AutomationProperties.Name="Tree" 
             ItemsSource="{Binding}">
                <controls:TreeViewEx.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding}"  DataType="{x:Type customControls:SongTreeViewItem}">
                        <TextBlock Text="{Binding}"/>
                    </HierarchicalDataTemplate>
                </controls:TreeViewEx.ItemTemplate>
                <controls:TreeViewEx.ContextMenu>
                    <ContextMenu StaysOpen="True">
                        <MenuItem Header="{StaticResource ContextMenu-AutoIdMusic-Header}"
                                  Click="ContextMenu_AutoIDMusic_Click" />
                    </ContextMenu>
                </controls:TreeViewEx.ContextMenu>
            </controls:TreeViewEx>

            <Grid Grid.Column="2" Margin="0,33,0,0" Background="Transparent">
                <Border BorderThickness="1" BorderBrush="Gray" >
                    <StackPanel >
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>


        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Slider />
            <StackPanel Height="64" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Previous" MinWidth="75" Click="Button_Click" />
                <Button Content="Play" MinWidth="75" />
                <Button Content="Skip" MinWidth="75" />
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.Column="0">
            <StatusBar>
                <StatusBar.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </StatusBar.ItemsPanel>
                <StatusBarItem Grid.Column="1">
                    <TextBlock Name="StatusMessage"></TextBlock>
                </StatusBarItem>
                <StatusBarItem Grid.Column="2">
                    <Image Name="StatusIcon"/>
                </StatusBarItem>
            </StatusBar>
        </StackPanel>
    </Grid>
</Window>