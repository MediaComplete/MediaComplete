<Window x:Class="MSOE.MediaComplete.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:System.Windows.Controls;assembly=TreeViewEx" 
        Title="Media Complete" Height="339" Width="787">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MainWindowResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition Height="24" />
            <RowDefinition />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Edit" Click="MenuItem_Click">
                <MenuItem Header="_Settings" />
            </MenuItem>
            <MenuItem Header="_Playlists" />
            <MenuItem Header="_MetaDataFinderThing" />
        </Menu>
        <TextBox Grid.Row="0" Text="Search" Width="200" HorizontalAlignment="Right" />
        <ToolBarTray Grid.Row="1">
            <ToolBar>
                <Button Name="AddFile" ToolTip="Add File" VerticalAlignment="Center" Click="AddFile_Click">
                    <Image Source="{StaticResource Toolbar-AddMusicFile-Icon}" />
                </Button>
                <Button Name="AddFolder" ToolTip="Add Folder" VerticalAlignment="Center" Click="AddFolder_Click">
                    <Image Source="{StaticResource Toolbar-AddMusicFolder-Icon}" />
                </Button>
                <Button Name="AutoIdentifyMusic" ToolTip="{StaticResource Toolbar-AutoIdMusic-Tooltip}"
                        VerticalAlignment="Center" Click="Toolbar_AutoIDMusic_Click">
                    <Image Source="{StaticResource Toolbar-AutoIdMusic-Icon}" />
                </Button>
                <Button Name="SortMusic" ToolTip="{StaticResource Toolbar-SortMusic-Tooltip}"
                        VerticalAlignment="Center" Click="Toolbar_SortMusic_Click">
                    <Image Source="{StaticResource Toolbar-SortMusic-Icon}" />
                </Button>
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*" />
                <ColumnDefinition Width="100*" />
                <ColumnDefinition Width="100*" />
            </Grid.ColumnDefinitions>
            <controls:TreeViewEx x:Name="LibraryTree" Grid.Column="0" AutomationProperties.Name="LeftTree">
                <controls:TreeViewEx.Resources>
                    <!-- Disabling warnings below - these resources are accessed from code, so Resharper doesn't think we're using them. -->
                    <!-- ReSharper disable once Xaml.RedundantResource -->
                    <ContextMenu x:Key="SongContextMenu" StaysOpen="True">
                        <MenuItem Header="{StaticResource ContextMenu-AutoIdMusic-Header}"
                                  Click="ContextMenu_AutoIDMusic_Click" />
                    </ContextMenu>
                    <!-- ReSharper disable once Xaml.RedundantResource -->
                    <ContextMenu x:Key="FolderContextMenu" StaysOpen="True" />
                </controls:TreeViewEx.Resources>
            </controls:TreeViewEx>
            <TreeView Grid.Column="1" />
            <TreeView Grid.Column="2" />
        </Grid>

        <StackPanel Grid.Row="3">
            <Slider/>
            <StackPanel Height="64" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Previous" MinWidth="75" Click="Button_Click" />
                <Button Content="Play" MinWidth="75" />
                <Button Content="Skip" MinWidth="75" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
